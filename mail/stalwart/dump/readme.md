# Response dump

A dump of various responses from jmap_test.py script. These can be re-used as test fixtures or references. We'll clean them up later.

It's good to know that json has a standard response object, I just didn't map it out for whatever reason...
-Mel
